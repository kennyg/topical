[tools]
node = "22"
pnpm = "latest"

[tasks]
dev = "pnpm dev"
build = "pnpm build"
preview = "pnpm preview"
fetch-data = "pnpm fetch-data"
pages = "pnpm build:static && pnpm pages"
"pages:preview" = "pnpm build:static && pnpm pages:preview"
